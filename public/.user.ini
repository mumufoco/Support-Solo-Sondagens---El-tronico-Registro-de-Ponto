; PHP-FPM Configuration for Ponto Eletrônico
; This file is automatically read by PHP-FPM (cPanel/shared hosting)

; Display errors - OFF in production
display_errors = Off
log_errors = On

; Upload limits
upload_max_filesize = 64M
post_max_size = 64M

; Execution limits
max_execution_time = 300
max_input_time = 300
memory_limit = 256M

; Session configuration REMOVED
; Session settings are managed by CodeIgniter via ini_set() in app/Config/Session.php
; Any session configuration here will conflict with CodeIgniter's session management
