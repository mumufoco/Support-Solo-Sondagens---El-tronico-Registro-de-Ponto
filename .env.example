#--------------------------------------------------------------------
# ENVIRONMENT
#--------------------------------------------------------------------

CI_ENVIRONMENT = development

#--------------------------------------------------------------------
# APP
#--------------------------------------------------------------------

app.baseURL = 'http://localhost:8080/'
app.appTimezone = 'America/Sao_Paulo'
app.defaultLocale = 'pt-BR'
app.supportedLocales = ['pt-BR']

# app.forceGlobalSecureRequests = true

#--------------------------------------------------------------------
# DATABASE
#--------------------------------------------------------------------

database.default.hostname = localhost
database.default.database = ponto_eletronico
database.default.username = root
database.default.password =
database.default.DBDriver = MySQLi
database.default.DBPrefix =
database.default.port = 3306
database.default.charset = utf8mb4
database.default.DBCollat = utf8mb4_general_ci

# database.tests.hostname = localhost
# database.tests.database = ponto_eletronico_test
# database.tests.username = root
# database.tests.password =
# database.tests.DBDriver = MySQLi
# database.tests.port = 3306

#--------------------------------------------------------------------
# SECURITY
#--------------------------------------------------------------------

# encryption.key =
# Gere com: php spark key:generate

security.csrfProtection = 'session'
security.tokenRandomize = true
security.tokenName = 'csrf_token_name'
security.headerName = 'X-CSRF-TOKEN'
security.cookieName = 'csrf_cookie_name'
security.expires = 7200
security.regenerate = true

#--------------------------------------------------------------------
# ENCRYPTION (Settings Encryption)
#--------------------------------------------------------------------

# Generate with: php spark encryption:generate-key
# WARNING: Keep this key secure! Loss means loss of encrypted data.
# ENCRYPTION_KEY =
# ENCRYPTION_KEY_VERSION = 1

#--------------------------------------------------------------------
# RATE LIMITING
#--------------------------------------------------------------------

# Comma-separated list of whitelisted IPs (no rate limiting)
# Example: RATE_LIMIT_WHITELIST = '192.168.1.100,10.0.0.50'
# Note: Localhost (127.0.0.1 and ::1) is always whitelisted
# RATE_LIMIT_WHITELIST =

#--------------------------------------------------------------------
# SECURITY HEADERS
#--------------------------------------------------------------------

# Custom Content Security Policy (CSP)
# Example: SECURITY_CSP = "default-src 'self'; script-src 'self' https://cdn.example.com"
# Leave empty to use default strict CSP
# SECURITY_CSP =

# Referrer Policy
# Options: no-referrer, no-referrer-when-downgrade, origin, origin-when-cross-origin,
#          same-origin, strict-origin, strict-origin-when-cross-origin, unsafe-url
# Default: strict-origin-when-cross-origin
# SECURITY_REFERRER_POLICY = strict-origin-when-cross-origin

# Allow frames from same origin (set to 'true' to enable)
# Default: false (DENY all frames)
# SECURITY_ALLOW_FRAMES = false

#--------------------------------------------------------------------
# SESSION
#--------------------------------------------------------------------

session.driver = 'CodeIgniter\Session\Handlers\FileHandler'
session.cookieName = 'ponto_session'
session.expiration = 7200
session.savePath = WRITEPATH . 'session'
session.matchIP = false
session.timeToUpdate = 300
session.regenerateDestroy = false

#--------------------------------------------------------------------
# DEEPFACE API
#--------------------------------------------------------------------

DEEPFACE_API_URL = 'http://localhost:5000'
DEEPFACE_THRESHOLD = 0.40
DEEPFACE_MODEL = 'VGG-Face'
DEEPFACE_DETECTOR = 'opencv'
DEEPFACE_ANTI_SPOOFING = true

#--------------------------------------------------------------------
# SOURCEAFIS - Biometria Digital (Fingerprint)
#--------------------------------------------------------------------

# Modes: 'api' (SourceAFIS REST API), 'native' (PHP minutiae), 'mock' (dev)
SOURCEAFIS_MODE = 'native'
SOURCEAFIS_API_URL = 'http://localhost:8081'
SOURCEAFIS_TIMEOUT = 30
SOURCEAFIS_THRESHOLD = 0.40
SOURCEAFIS_ENABLED = false

#--------------------------------------------------------------------
# GEOLOCATION
#--------------------------------------------------------------------

NOMINATIM_URL = 'https://nominatim.openstreetmap.org'
NOMINATIM_EMAIL = 'admin@example.com'
GEOFENCE_DEFAULT_RADIUS = 100

#--------------------------------------------------------------------
# FILE UPLOAD
#--------------------------------------------------------------------

UPLOAD_MAX_SIZE = 5242880
UPLOAD_ALLOWED_TYPES = 'jpg|jpeg|png|pdf'

#--------------------------------------------------------------------
# EMAIL
#--------------------------------------------------------------------

email.protocol = 'smtp'
email.SMTPHost = 'smtp.gmail.com'
email.SMTPUser = 'seu-email@gmail.com'
email.SMTPPass = 'sua-senha-app'
email.SMTPPort = 587
email.SMTPCrypto = 'tls'
email.mailType = 'html'
email.charset = 'utf-8'
email.fromEmail = 'noreply@pontoeletronico.com.br'
email.fromName = 'Sistema Ponto Eletrônico'

#--------------------------------------------------------------------
# ICP-BRASIL (OPCIONAL)
#--------------------------------------------------------------------

# ICP_CERTIFICATE_PATH = WRITEPATH . 'keys/icp/certificate.pem'
# ICP_PRIVATE_KEY_PATH = WRITEPATH . 'keys/icp/private.key'
# ICP_PRIVATE_KEY_PASSWORD = ''

#--------------------------------------------------------------------
# LGPD
#--------------------------------------------------------------------

LGPD_DPO_NAME = 'Nome do DPO'
LGPD_DPO_EMAIL = 'dpo@empresa.com.br'
LGPD_DATA_RETENTION_DAYS = 3650

#--------------------------------------------------------------------
# COMPANY INFO
#--------------------------------------------------------------------

COMPANY_NAME = 'Empresa Exemplo LTDA'
COMPANY_CNPJ = '00.000.000/0001-00'
COMPANY_ADDRESS = 'Rua Exemplo, 123 - Centro - São Paulo/SP'
COMPANY_PHONE = '(11) 1234-5678'

#--------------------------------------------------------------------
# CACHE
#--------------------------------------------------------------------

cache.handler = 'file'
# cache.handler = 'redis'
# cache.storePath = WRITEPATH . 'cache/'
# cache.redis.host = '127.0.0.1'
# cache.redis.port = 6379
# cache.redis.password = null
# cache.redis.database = 0

#--------------------------------------------------------------------
# PUSH NOTIFICATIONS (Web Push / VAPID)
#--------------------------------------------------------------------

# VAPID Subject (mailto: or https:// URL)
VAPID_SUBJECT = 'mailto:admin@pontoeletronico.com.br'

# VAPID Public Key
# Generate keys using: npx web-push generate-vapid-keys
# Or install globally: npm install -g web-push && web-push generate-vapid-keys
VAPID_PUBLIC_KEY = ''

# VAPID Private Key
VAPID_PRIVATE_KEY = ''

#--------------------------------------------------------------------
# LOGGING
#--------------------------------------------------------------------

logger.threshold = 4
